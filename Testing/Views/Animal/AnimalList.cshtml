@*var obj = {
        AnimalCD: '',
    };
    var response = CalltoApiController('@Url.Action("GetAnimal", "api/AnimalApi")', obj);
    alert(response)*@

@section Scripts
{
    <script type="text/javascript">
        $(document).ready(function () {
                var obj = {
                AnimalCD: '',
            };
            var response1 = CalltoApiController('@Url.Action("GetAnimal", "api/AnimalApi")', obj);
            alert(response1)
            var a = {
                AnimalCD:"a4",
                AnimalName: "Ant",
                Description:"CKM",
                Mode: "New"
            };
            var response = CalltoApiController('@Url.Action("AnimalCUD", "api/AnimalApi")', a);
            alert(response);
        });


    </script>
}

