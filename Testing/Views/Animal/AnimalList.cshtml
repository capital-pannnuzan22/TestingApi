
@section Scripts
{
    <script type="text/javascript">
        $(document).ready(function () {
            if ('Mode' == 'Insert' || 'Mode' == 'Update' || 'Mode' == 'Delete') {
                let set = {
                    AnimalCD: "a1",
                    AnimalName: "Antt",
                    Description: "CKM",
                    Mode: "New"
                };
               let response1 = CalltoApiController('@Url.Action("AnimalCUD", "api/AnimalApi")', set);
                alert(response1);

                @*let put = {
                    AnimalCD: "a7",
                    AnimalName: "Ant",
                    Description: "CKM",
                    Mode: "Update"
                };
                let response2 = CalltoApiController('@Url.Action("AnimalCUD", "api/AnimalApi")', put);
                alert(response2);

                let del = {
                    AnimalCD: "a1",
                    AnimalName: "Ant",
                    Description: "CKM",
                    Mode: "Delete"
                };
                let response3 = CalltoApiController('@Url.Action("AnimalCUD", "api/AnimalApi")', del);
                alert(response3);*@
            }

            else {
                alert("No Mode");
            }

            @*let get = {
                AnimalCD: '',
            };
            let response = CalltoApiController('@Url.Action("GetAnimal", "api/AnimalApi")', get);
            alert(response)*@



        });


    </script>
}

